{
  "name": "simionic-custom-profile-manager",
  "version": "1.0.5",
  "description": "Manage Simionic G1000 custom aircraft profiles on connected iPads",
  "main": "dist/main/main/index.js",
  "scripts": {
    "dev": "npm run compile && electron .",
    "compile": "tsc -p tsconfig.main.json && tsc -p tsconfig.preload.json && tsc -p tsconfig.renderer.json",
    "build": "npm run compile && electron-builder",
    "postinstall": "electron-builder install-app-deps"
  },
  "author": "Simionic",
  "license": "MIT",
  "devDependencies": {
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "typescript": "^5.7.0"
  },
  "dependencies": {
    "appium-ios-device": "^2.8.0",
    "sql.js": "^1.12.0"
  }
}
